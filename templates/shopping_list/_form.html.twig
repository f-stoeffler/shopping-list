{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    {% if shopping_list.id %}
    <a class="btn btn-outline-danger" href="{{ path('app_shopping_list_items_edit', {'id': shopping_list.id}) }}">Cancel</a>
    {% else %}
    <a class="btn btn-outline-danger" href="{{ path('app_shopping_list_index') }}">Cancel</a>
    {% endif %}
{{ form_end(form) }}
